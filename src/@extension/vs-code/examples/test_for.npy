// Test: Boucle For standard
total: int = 0;

for (i: int = 0; i < 10; i++) {
    total += i;
}

// Avec condition implicite (variable existante)
j: int = 0;
for (j = 0; j < 5; j++) {
    total += 1;
}