func isPow2(n: int) -> bool {
    if (n <= 0) { ret False; }
    x: int = n;
    while ((x % 2) == 0) { x = x / 2; }
    ret x == 1;
}

func nextPow2(n: int) -> int {
    if (n <= 1) { ret 1; }
    x: int = 1;
    while (x < n) { x = x * 2; }
    ret x;
}
